# Core
python-dotenv>=1.0.1
Flask>=3.0.3

# LiveKit SDKs
livekit~=1.0.12
livekit-api~=1.0.5

# Agents + OpenAI plugin (pulled in via the extras tag)
livekit-agents[openai]~=1.2.6

# If you prefer to pin the plugin explicitly instead of using [openai], use:
# livekit-agents~=1.2.6
# livekit-plugins-openai~=1.2.7

# Optional: only if you must run on Python < 3.9
# backports.zoneinfo; python_version<"3.9"